<section class="confidenceSection" aria-labelledby="confidence-title">
  <div class="sectionShell">
    <header class="sectionHeader">
      <p class="eyebrow">Private arrival, thoughtfully arranged</p>

      <h2 id="confidence-title">
        Confidence begins
        <span>before landing.</span>
      </h2>

      <p class="intro">
        Arriving somewhere unfamiliar should not begin with uncertainty.
        Compare verified private transfer partners, understand what is included
        and arrange the essential details before your flight lands.
      </p>
    </header>

    <div class="storyGrid">
      <div class="visual">
        <picture>
          <source
            media="(max-width: 700px)"
            srcset="/images/why-private-transfer/why-private-transfer-mobile.webp"
          />

          <img
            src="/images/why-private-transfer/why-private-transfer.webp"
            alt="A premium private arrival experience arranged before landing"
            width="1600"
            height="1200"
            loading="lazy"
            decoding="async"
          />
        </picture>

        <div class="visualShade"></div>

        <div class="visualMessage">
          <p>Everything is already waiting.</p>
          <span>Your arrival, without improvisation.</span>
        </div>
      </div>

      <div class="benefits">
        <article>
          <div class="iconWrap" aria-hidden="true">
            <svg viewBox="0 0 24 24">
              <path
                d="M12 3 5.5 5.6v5.2c0 4.3 2.7 8.1 6.5 9.4 3.8-1.3 6.5-5.1 6.5-9.4V5.6L12 3Z"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linejoin="round"
              />
              <path
                d="m9.2 11.8 1.8 1.8 3.8-4"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </div>

          <div>
            <p class="benefitNumber">01</p>
            <h3>Know who is meeting you</h3>
            <p>
              Review the transfer company, pickup instructions and available
              service details before your journey begins.
            </p>
          </div>
        </article>

        <article>
          <div class="iconWrap" aria-hidden="true">
            <svg viewBox="0 0 24 24">
              <path
                d="M4 7.5h16v9H4z"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linejoin="round"
              />
              <path
                d="M8 12h8M7 7.5V6h10v1.5"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linecap="round"
              />
            </svg>
          </div>

          <div>
            <p class="benefitNumber">02</p>
            <h3>Know what you will pay</h3>
            <p>
              Compare fares and included services before booking, with fewer
              unknowns after a long flight.
            </p>
          </div>
        </article>

        <article>
          <div class="iconWrap" aria-hidden="true">
            <svg viewBox="0 0 24 24">
              <path
                d="M5 15.5h14M7 15.5l1.3-5.2a2 2 0 0 1 1.9-1.5h3.6a2 2 0 0 1 1.9 1.5l1.3 5.2M7 15.5v2M17 15.5v2"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </div>

          <div>
            <p class="benefitNumber">03</p>
            <h3>Choose the right vehicle</h3>
            <p>
              Match the transfer to your passengers, luggage, child seats,
              group size and preferred level of comfort.
            </p>
          </div>
        </article>

        <article>
          <div class="iconWrap" aria-hidden="true">
            <svg viewBox="0 0 24 24">
              <path
                d="M12 21s6-5.2 6-11a6 6 0 1 0-12 0c0 5.8 6 11 6 11Z"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
                stroke-linejoin="round"
              />
              <circle
                cx="12"
                cy="10"
                r="2.2"
                fill="none"
                stroke="currentColor"
                stroke-width="1.7"
              />
            </svg>
          </div>

          <div>
            <p class="benefitNumber">04</p>
            <h3>Arrive at the correct destination</h3>
            <p>
              Share your hotel, resort or address in advance and reduce
              misunderstandings when you arrive.
            </p>
          </div>
        </article>
      </div>
    </div>

    <footer class="sectionFooter">
      <div>
        <p class="footerLabel">A calmer way to arrive</p>
        <strong>Prepared before takeoff. Clear when you land.</strong>
      </div>

      <a class="primaryAction" href="#transfer-search">
        Arrange my arrival

        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path
            d="M7 17 17 7M8 7h9v9"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
      </a>
    </footer>
  </div>
</section>

<style>
  .confidenceSection {
    padding: 56px 30px 112px;
    background:
      radial-gradient(circle at 8% 8%, rgba(225, 216, 198, 0.3), transparent 32%),
      linear-gradient(180deg, #f6f7f8 0%, #eef1f4 100%);
  }

  .sectionShell {
    max-width: 1400px;
    margin: 0 auto;
  }

  .sectionHeader {
    max-width: 850px;
    margin-bottom: 48px;
  }

  .eyebrow,
  .footerLabel,
  .benefitNumber {
    margin: 0;
    color: #315b8c;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 0.17em;
    text-transform: uppercase;
  }

  h2 {
    margin: 15px 0 0;
    color: #101827;
    font-size: clamp(46px, 5vw, 72px);
    font-weight: 760;
    line-height: 0.94;
    letter-spacing: -0.065em;
    text-wrap: balance;
  }

  h2 span {
    display: block;
    color: #6f7784;
    font-family: Georgia, "Times New Roman", serif;
    font-weight: 400;
    font-style: italic;
    letter-spacing: -0.045em;
  }

  .intro {
    max-width: 720px;
    margin: 24px 0 0;
    color: #657083;
    font-size: 17px;
    line-height: 1.7;
  }

  .storyGrid {
    display: grid;
    grid-template-columns: minmax(0, 1.12fr) minmax(420px, 0.88fr);
    gap: 34px;
    align-items: stretch;
  }

  .visual {
    position: relative;
    min-height: 660px;
    overflow: hidden;
    border-radius: 30px;
    background: #dfe4e8;
    box-shadow: 0 28px 80px rgba(15, 23, 42, 0.12);
  }

  .visual picture,
  .visual img {
    width: 100%;
    height: 100%;
    display: block;
  }

  .visual img {
    object-fit: cover;
    object-position: center;
  }

  .visualShade {
  position: absolute;
  inset: 0;

  background:
    linear-gradient(
      180deg,
      rgba(8, 14, 23, 0.02) 45%,
      rgba(8, 14, 23, 0.36) 100%
    );
}

  .visualMessage {
    position: absolute;
    left: 34px;
    right: 34px;
    bottom: 32px;
    color: white;
  }

  .visualMessage p {
    margin: 0;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(30px, 3vw, 48px);
    font-style: italic;
    line-height: 1;
    letter-spacing: -0.045em;
  }

  .visualMessage span {
    display: block;
    margin-top: 11px;
    font-size: 13px;
    font-weight: 650;
    letter-spacing: 0.02em;
    opacity: 0.88;
  }

  .benefits {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
  }

  article {
    min-width: 0;
    display: grid;
    grid-template-columns: 54px 1fr;
    gap: 19px;
    padding: 27px;
    border: 1px solid rgba(15, 23, 42, 0.075);
    border-radius: 24px;
    background: rgba(255, 255, 255, 0.88);
    box-shadow:
      0 2px 4px rgba(15, 23, 42, 0.018),
      0 14px 36px rgba(15, 23, 42, 0.045);
    backdrop-filter: blur(18px);
    transition:
      transform 220ms ease,
      border-color 220ms ease,
      box-shadow 220ms ease;
  }

  article:hover {
    transform: translateY(-3px);
    border-color: rgba(49, 91, 140, 0.16);
    box-shadow:
      0 2px 5px rgba(15, 23, 42, 0.02),
      0 24px 54px rgba(15, 23, 42, 0.08);
  }

  .iconWrap {
    width: 54px;
    height: 54px;
    display: grid;
    place-items: center;
    color: #315b8c;
    border: 1px solid rgba(49, 91, 140, 0.1);
    border-radius: 17px;
    background:
      linear-gradient(145deg, rgba(247, 250, 253, 1), rgba(235, 241, 247, 1));
  }

  .iconWrap svg {
    width: 24px;
    height: 24px;
  }

  .benefitNumber {
    margin-bottom: 7px;
    color: #8993a2;
    font-size: 8px;
  }

  h3 {
    margin: 0;
    color: #101827;
    font-size: 21px;
    font-weight: 760;
    line-height: 1.1;
    letter-spacing: -0.035em;
  }

  article p:last-child {
    margin: 10px 0 0;
    color: #6b7687;
    font-size: 13px;
    line-height: 1.6;
  }

  .sectionFooter {
    margin-top: 34px;
    padding: 26px 28px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 30px;
    border: 1px solid rgba(15, 23, 42, 0.075);
    border-radius: 24px;
    background: rgba(255, 255, 255, 0.88);
  }

  .sectionFooter strong {
    display: block;
    margin-top: 7px;
    color: #111b2d;
    font-size: 20px;
    font-weight: 760;
    letter-spacing: -0.035em;
  }

  .primaryAction {
    flex: 0 0 auto;
    min-height: 48px;
    display: inline-flex;
    align-items: center;
    gap: 11px;
    padding: 0 8px 0 18px;
    color: #183654;
    border: 1px solid rgba(24, 54, 84, 0.14);
    border-radius: 999px;
    background: white;
    box-shadow: 0 10px 26px rgba(15, 23, 42, 0.07);
    font-size: 12px;
    font-weight: 850;
    transition:
      transform 200ms ease,
      box-shadow 200ms ease;
  }

  .primaryAction svg {
    width: 34px;
    height: 34px;
    padding: 9px;
    color: white;
    border-radius: 50%;
    background: #183f70;
  }

  .primaryAction:hover {
    transform: translateY(-2px);
    box-shadow: 0 15px 34px rgba(15, 23, 42, 0.1);
  }

  @media (max-width: 1100px) {
    .storyGrid {
      grid-template-columns: 1fr;
    }

    .visual {
      min-height: 560px;
    }

    .benefits {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 720px) {
    .confidenceSection {
      padding: 78px 16px;
    }

    .sectionHeader {
      margin-bottom: 30px;
    }

    h2 {
      font-size: clamp(42px, 12vw, 56px);
    }

    .intro {
      margin-top: 20px;
      font-size: 15px;
      line-height: 1.65;
    }

    .visual {
      min-height: 520px;
      border-radius: 24px;
    }

    .visual img {
      object-position: center top;
    }

    .visualMessage {
      left: 22px;
      right: 22px;
      bottom:14%;
    }

    .visualMessage p {
      font-size: 34px;
    }

    .benefits {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    article {
      grid-template-columns: 48px 1fr;
      gap: 15px;
      padding: 22px 19px;
      border-radius: 20px;
    }

    .iconWrap {
      width: 48px;
      height: 48px;
      border-radius: 15px;
    }

    .iconWrap svg {
      width: 22px;
      height: 22px;
    }

    h3 {
      font-size: 20px;
    }

    article p:last-child {
      font-size: 13px;
    }

    .sectionFooter {
      padding: 23px 20px;
      align-items: flex-start;
      flex-direction: column;
      border-radius: 20px;
    }

    .sectionFooter strong {
      font-size: 19px;
      line-height: 1.25;
    }

    .primaryAction {
      width: 100%;
      justify-content: space-between;
      min-height: 52px;
      padding-left: 17px;
      font-size: 12px;
    }
  }

  @media (max-width: 390px) {
    .visual {
      min-height: 480px;
    }

    article {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    article,
    .primaryAction {
      transition: none;
    }

    article:hover,
    .primaryAction:hover {
      transform: none;
    }
  }
</style>
