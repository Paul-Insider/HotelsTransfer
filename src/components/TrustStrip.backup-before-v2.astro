<section class="trustStrip" aria-label="HotelsTransfer benefits">
  <article>
    <span class="marker"></span>
    <div>
      <strong>Verified operators</strong>
      <p>Every company is reviewed before appearing on the platform.</p>
    </div>
  </article>

  <article>
    <span class="marker"></span>
    <div>
      <strong>Transparent prices</strong>
      <p>Compare transfer prices without hidden surprises.</p>
    </div>
  </article>

  <article>
    <span class="marker"></span>
    <div>
      <strong>Worldwide coverage</strong>
      <p>Airport transfers across leading global destinations.</p>
    </div>
  </article>

  <article>
    <span class="marker"></span>
    <div>
      <strong>Secure booking</strong>
      <p>Your reservation and personal information remain protected.</p>
    </div>
  </article>
</section>

<style>
  .trustStrip {
    position: relative;
    z-index: 4;
    max-width: 1240px;
    margin: -28px auto 0;
    padding: 22px 26px;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 24px;
    border: 1px solid rgba(15, 23, 42, 0.08);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.97);
    box-shadow: 0 20px 55px rgba(15, 23, 42, 0.1);
  }

  article {
    min-width: 0;
    display: flex;
    gap: 14px;
    align-items: flex-start;
  }

  .marker {
    flex: 0 0 auto;
    width: 8px;
    height: 8px;
    margin-top: 5px;
    border-radius: 50%;
    background: var(--blue);
    box-shadow: 0 0 0 5px rgba(11, 78, 162, 0.08);
  }

  strong {
    display: block;
    margin-bottom: 6px;
    font-size: 13px;
    font-weight: 850;
  }

  p {
    margin: 0;
    color: var(--muted);
    font-size: 11px;
    line-height: 1.55;
  }

  @media (max-width: 980px) {
    .trustStrip {
      margin-inline: 20px;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 600px) {
    .trustStrip {
      margin: -18px 14px 0;
      padding: 20px;
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }
</style>
