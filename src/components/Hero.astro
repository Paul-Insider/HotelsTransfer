<section class="hero">
  <picture class="heroMedia">
    <source
      media="(max-width: 767px)"
      srcset="/images/hero/hero-global-mobility-mobile.webp"
    />

    <img
      src="/images/hero/hero-global-mobility.webp"
      alt=""
      width="2400"
      height="1350"
      loading="eager"
      fetchpriority="high"
      decoding="async"
    />
  </picture>

  <div class="heroOverlay"></div>

  <header class="nav">
    <a class="brand" href="/" aria-label="HotelsTransfer home">
      <img
        src="/images/logo/hotels-transfer-logo.png"
        alt="HotelsTransfer"
        width="1200"
        height="220"
        decoding="async"
      />
    </a>

    <nav aria-label="Primary navigation">
      <a href="/destinations">Destinations</a>
      <a href="/airports">Airports</a>
      <a href="/companies">Private Partners</a>
      <a href="/how-it-works">How it works</a>
    </nav>

    <div class="navActions">
  <a class="foundingPartnerAction" href="/partners/apply">
    <span class="desktopLabel">Partner</span>
    <span class="mobileLabel">Founding Partner</span>
  </a>

  <a class="login" href="/login">
    Log in
  </a>

  <a class="bookButton" href="#transfer-search">
    Book a transfer
  </a>

  <button
    class="menuToggle"
    type="button"
    aria-label="Open menu"
    aria-expanded="false"
    aria-controls="mobileMenu"
  >
    <span></span>
    <span></span>
    <span></span>
  </button>
</div>
  </header>

  <div class="menuBackdrop" data-menu-close></div>

<aside
  class="mobileMenu"
  id="mobileMenu"
  aria-hidden="true"
>
  <div class="mobileMenuHeader">
    <a class="mobileMenuBrand" href="/" aria-label="HotelsTransfer home">
      <img
        src="/images/logo/hotels-transfer-logo.png"
        alt="HotelsTransfer"
      />
    </a>

    <button
      class="menuClose"
      type="button"
      aria-label="Close navigation menu"
      data-menu-close
    >
      <span></span>
      <span></span>
    </button>
  </div>

  <div class="mobileMenuIntro">
    <span>Private mobility</span>
    <p>Exceptional transfers, wherever your journey begins.</p>
  </div>

  <nav class="mobileMenuNavigation" aria-label="Mobile navigation">
    <a href="/destinations" data-menu-close>
      <span>Destinations</span>
      <span class="menuArrow">↗</span>
    </a>

    <a href="#transfer-search" data-menu-close>
      <span>Book a transfer</span>
      <span class="menuArrow">↗</span>
    </a>

    <div class="mobileMenuDivider"></div>

    <a href="/login" data-menu-close>
      <span>Log in</span>
      <span class="menuArrow">↗</span>
    </a>

    <a href="/about" data-menu-close>
      <span>About</span>
      <span class="menuArrow">↗</span>
    </a>

    <a href="/support" data-menu-close>
      <span>Support</span>
      <span class="menuArrow">↗</span>
    </a>
  </nav>

  <div class="mobileMenuFooter">
    <span class="mobileMenuFooterLabel">
      For private transfer companies
    </span>

    <a
      class="mobilePartnerLink"
      href="/partners/apply"
      data-menu-close
    >
      Become a Founding Private Partner
      <span>→</span>
    </a>
  </div>
</aside>

  <div class="heroContent">
    <p class="eyebrow">The world’s private mobility network</p>

    <h1>
      Compare private airport transfers
      <span>in over 1,100 destinations</span>
    </h1>

    <p class="lead">
      Trusted local companies, clear prices and professional drivers, from
      airport arrival to hotel door
    </p>

    <form
      id="transfer-search"
      class="searchCapsule"
      aria-label="Search airport transfers"
    >
      <label class="searchField pickupField">
        <span>Pickup airport</span>
        <input
          type="search"
          name="pickup"
          placeholder="Select pickup airport"
          autocomplete="off"
        />
      </label>

      <label class="searchField destinationField">
        <span>Destination</span>
        <input
          type="search"
          name="destination"
          placeholder="Hotel, address or destination"
          autocomplete="off"
        />
      </label>

      <label class="searchField dateField">
        <span>Date</span>
        <input type="date" name="date" />
      </label>

      <label class="searchField passengersField">
        <span>Passengers</span>
        <select name="passengers">
          <option value="1">1 passenger</option>
          <option value="2">2 passengers</option>
          <option value="3">3 passengers</option>
          <option value="4">4 passengers</option>
          <option value="5">5+ passengers</option>
        </select>
      </label>

      <button type="submit">
        <span>Search transfers</span>
        <span class="searchArrow" aria-hidden="true">→</span>
      </button>
    </form>

    <ul class="trustIndicators" aria-label="Transfer service highlights">
  <li>Professional drivers</li>
  <li>Fixed pricing</li>
  <li>Flight monitored</li>
  <li>Meet &amp; greet</li>
</ul>
  </div>
</section>

<style>
  .hero {
  position: relative;
  min-height: 760px;
  padding: 0 28px 58px;
  overflow: hidden;
  background: #182027;
}

  .heroMedia {
  position: absolute;
  inset: 0;
  z-index: 0;
}

  .heroMedia,
  .heroMedia img {
    width: 100%;
    height: 100%;
    display: block;
  }

  .heroMedia img {
    object-fit: cover;
    object-position: center;
  }

  .heroOverlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(
        180deg,
        rgba(10, 12, 14, 0.46) 0%,
        rgba(10, 13, 16, 0.18) 27%,
        rgba(8, 14, 20, 0.2) 58%,
        rgba(7, 13, 20, 0.58) 100%
      ),
      linear-gradient(90deg, rgba(6, 10, 14, 0.12), transparent 42%, rgba(6, 10, 14, 0.08));
  }

  .nav {
  position: fixed;
  top: 0;
  left: 28px;
  right: 28px;
  z-index: 1350;

  width: auto;
  max-width: 1380px;
  height: 68px;
  margin: 0 auto;
  padding: 0 4px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  color: #fff;

  border: 1px solid rgba(200, 166, 96, 0);

  transition:
  top 250ms ease,
  height 250ms ease,
  padding 250ms ease,
  background-color 250ms ease,
  border-color 250ms ease,
  border-radius 250ms ease,
  box-shadow 250ms ease;
}

.nav::after {
  content: "";
  position: absolute;
  left: 4px;
  right: 4px;
  bottom: 0;

  height: 1px;
  background: rgba(255, 255, 255, 0.24);

  opacity: 1;
  pointer-events: none;

  transition: opacity 120ms ease;
}

.nav.isScrolled::after {
  opacity: 0;
}

  .nav.isScrolled {
  top: 10px;
  height: 58px;
  padding: 0 18px;

  border: 1px solid rgba(200, 166, 96, 0.22);
  border-radius: 18px;

  background: rgba(10, 16, 22, 0.82);

  box-shadow:
    0 16px 42px rgba(0, 0, 0, 0.18),
    0 0 0 1px rgba(200, 166, 96, 0.025),
    inset 0 1px 0 rgba(232, 207, 157, 0.13);

  backdrop-filter: blur(18px) saturate(135%);
  -webkit-backdrop-filter: blur(18px) saturate(135%);
}

  .brand {
    color: #fff;
    font-size: 18px;
    font-weight: 760;
    letter-spacing: -0.035em;
  }

  .nav nav,
  .navActions {
    display: flex;
    align-items: center;
  }

  .nav nav {
    gap: clamp(22px, 2.2vw, 34px);
    font-size: 13px;
    font-weight: 560;
  }

  .navActions {
    gap: 22px;
    font-size: 13px;
    font-weight: 650;
  }

  .nav nav a,
  .login {
    color: #fff;
    opacity: 0.88;
    transition: opacity 450ms ease;
  }

  .nav nav a:hover,
  .login:hover {
    opacity: 1;
  }

  .foundingPartnerAction {
    min-height: 36px;
    padding: 0 13px;
    display: inline-flex;
    align-items: center;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.34);
    border-radius: 8px;
    background: rgba(8, 18, 29, 0.2);
    font-size: 11px;
    font-weight: 700;
    white-space: nowrap;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .foundingPartnerAction:hover {
    border-color: rgba(255, 255, 255, 0.56);
    background: rgba(8, 18, 29, 0.32);
  }

  .bookButton {
    padding: 11px 18px;
    border-radius: 999px;
    color: #202326;
    background: #f7f6f2;
    box-shadow: 0 6px 20px rgba(8, 13, 19, 0.08);
    font-size: 12px;
    font-weight: 760;
    transition:
      transform 450ms cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 450ms ease,
      background-color 450ms ease;
  }

  .bookButton:hover {
    background: #fff;
    box-shadow: 0 10px 28px rgba(8, 13, 19, 0.16);
    transform: translateY(-1px);
  }

  .heroContent {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 124px auto 0;
    text-align: center;
    color: #fff;
  }

  .eyebrow {
    margin: 0 0 18px;
    font-size: 11px;
    font-weight: 720;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.85;
  }

  h1 {
    max-width: 1000px;
    margin: 0 auto;
    font-size: clamp(50px, 5.35vw, 76px);
    font-weight: 680;
    line-height: 0.99;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  h1 span {
    display: block;
    margin-top: 7px;
    font-family: inherit;
    font-weight: 360;
    letter-spacing: -0.05em;
  }

  .lead {
    max-width: 640px;
    margin: 24px auto 34px;
    font-size: 16px;
    line-height: 1.55;
    opacity: 0.82;
  }

  .searchCapsule {
    min-height: 80px;
    display: grid;
    grid-template-columns:
      minmax(170px, 1.25fr)
      minmax(180px, 1.25fr)
      minmax(120px, 0.7fr)
      minmax(125px, 0.72fr)
      minmax(176px, 0.85fr);
    align-items: stretch;
    padding: 8px;
    border: 1px solid rgba(255, 255, 255, 0.68);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.94);
    box-shadow:
      0 30px 90px rgba(4, 10, 17, 0.3),
      0 4px 18px rgba(4, 10, 17, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
  }

  .searchField {
    min-width: 0;
    padding: 11px 18px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: left;
  }

  .searchField + .searchField {
    border-left: 1px solid rgba(28, 34, 40, 0.12);
  }

  .searchField > span {
    display: block;
    margin-bottom: 5px;
    color: #777b7e;
    font-size: 9px;
    font-weight: 780;
    line-height: 1;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .searchField input,
  .searchField select {
    width: 100%;
    min-width: 0;
    min-height: 22px;
    padding: 0;
    border: 0;
    outline: 0;
    color: #24282b;
    background: transparent;
    font: inherit;
    font-size: 13px;
    font-weight: 650;
    line-height: 1.3;
  }

  .searchField input::placeholder {
    color: #4d5256;
    opacity: 1;
  }

  .searchField:focus-within {
    outline: 2px solid rgba(35, 40, 44, 0.34);
    outline-offset: -2px;
    border-radius: 999px;
  }

  .searchCapsule button {
    margin-left: 2px;
    padding: 0 21px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    border: 0;
    border-radius: 999px;
    color: #fff;
    background: #202326;
    box-shadow: 0 7px 20px rgba(16, 19, 22, 0.2);
    font-size: 12px;
    font-weight: 760;
    white-space: nowrap;
    cursor: pointer;
    transition:
      transform 500ms cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 500ms ease,
      background-color 500ms ease;
  }

  .searchCapsule button:hover {
    background: #111315;
    box-shadow: 0 11px 28px rgba(16, 19, 22, 0.25);
    transform: translateY(-1px);
  }

  .searchArrow {
    display: inline-block;
    font-size: 16px;
    font-weight: 400;
    transition: transform 500ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .searchCapsule button:hover .searchArrow {
    transform: translateX(3px);
  }

  .searchCapsule button:focus-visible,
  .bookButton:focus-visible,
  .nav a:focus-visible {
    outline: 2px solid #fff;
    outline-offset: 3px;
  }

  .trustIndicators {
    margin: 18px 0 0;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    list-style: none;
    color: rgba(255, 255, 255, 0.78);
    font-size: 11px;
    font-weight: 560;
    letter-spacing: 0.02em;
  }

  .trustIndicators li {
    display: flex;
    align-items: center;
  }

  .trustIndicators li + li::before {
    content: "";
    width: 3px;
    height: 3px;
    margin: 0 15px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.54);
  }

.mobileLabel,
.menuToggle,
.mobileMenu,
.menuBackdrop {
  display: none;
}

  @media (max-width: 1080px) {
  .nav nav {
    display: none;
  }

    .heroContent {
      margin-top: 114px;
    }

    .searchCapsule {
      grid-template-columns: 1.18fr 1.18fr 0.68fr 0.7fr 0.84fr;
    }

    .searchField {
      padding-inline: 14px;
    }
  }

  @media (max-width: 767px) {
    .hero {
      min-height: 1080px;
      padding: 0 14px 28px;
    }

    .heroMedia img {
      object-position: center top;
    }

    .heroOverlay {
      background:
        linear-gradient(
          180deg,
          rgba(12, 10, 9, 0.52) 0%,
          rgba(10, 13, 16, 0.18) 28%,
          rgba(8, 13, 18, 0.08) 48%,
          rgba(7, 13, 19, 0.72) 72%,
          rgba(7, 12, 18, 0.84) 100%
        );
    }

    .nav {
  left: 14px;
  right: 14px;

  height: 62px;
  padding: 0 2px;
}

.nav.isScrolled {
  top: 8px;
  height: 54px;
  padding: 0 10px;

  border-radius: 16px;
}

.brand {
  flex: 0 0 auto;
  min-width: 0;
  font-size: 16px;
}

.navActions {
  margin-left: auto;
  display: flex;
  align-items: center;
  gap: 8px;
}

.desktopLabel {
  display: none;
}

.mobileLabel {
  display: inline;
}

.login,
.bookButton {
  display: none;
}

.foundingPartnerAction {
  min-height: 34px;
  padding: 0 12px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  color: #ead9b5;
  border: 1px solid rgba(200, 166, 96, 0.58);
  border-radius: 999px;

  background: rgba(8, 13, 18, 0.24);

  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.07),
    0 5px 16px rgba(0, 0, 0, 0.11);

  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);

  font-size: 10.5px;
  font-weight: 650;
  line-height: 1;
  letter-spacing: 0.01em;
  white-space: nowrap;
  text-decoration: none;
}

.menuToggle {
  width: 34px;
  height: 34px;
  padding: 0;

  display: inline-flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;

  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 50%;

  background: rgba(8, 13, 18, 0.24);

  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);

  cursor: pointer;
}

.menuToggle span {
  display: block;
  width: 13px;
  height: 1px;

  border-radius: 999px;
  background: rgba(255, 255, 255, 0.95);
}

    .heroContent {
      margin-top: 62px;
    }

    .eyebrow {
      margin-bottom: 14px;
      font-size: 9px;
      letter-spacing: 0.17em;
    }

    h1 {
      max-width: 520px;
      font-size: clamp(40px, 11.5vw, 56px);
      line-height: 1;
      letter-spacing: -0.052em;
    }

    h1 span {
      margin-top: 6px;
      letter-spacing: -0.045em;
    }

    .lead {
      max-width: 370px;
      margin: 20px auto 250px;
      font-size: 14px;
      line-height: 1.55;
    }

    .searchCapsule {
      width: min(100%, 430px);
      min-height: 0;
      margin-inline: auto;
      padding: 4px 18px 6px;
      display: grid;
      grid-template-columns: 1fr;
      border-radius: 25px;
      background: rgba(255, 255, 255, 0.95);
      box-shadow:
        0 24px 64px rgba(4, 10, 17, 0.28),
        inset 0 1px 0 rgba(255, 255, 255, 0.96);
      backdrop-filter: blur(8px);
    }

    .searchField {
      min-height:56px;
      padding: 8px 4px;
    }

    .searchField + .searchField {
      border-top: 1px solid rgba(28, 34, 40, 0.11);
      border-left: 0;
    }

    .searchField:focus-within {
      border-radius: 8px;
    }

    .searchField input,
    .searchField select {
      font-size: 15px;
    }

    .searchCapsule button {
      min-height: 50px;
      margin: 5px 0 0;
      font-size: 13px;
    }

    .trustIndicators {
      width: min(100%, 370px);
      margin: 16px auto 0;
      row-gap: 8px;
      font-size: 10px;
    }

    .trustIndicators li + li::before {
      margin-inline: 9px;
    }
  }

  @media (max-width: 390px) {
    .hero {
      min-height: 1000px;
    }

    .lead {
      margin-bottom: 220px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav,
.nav a,
.bookButton,
.searchCapsule button,
.searchArrow {
  transition-duration: 0.01ms;
}

    .bookButton:hover,
    .searchCapsule button:hover,
    .searchCapsule button:hover .searchArrow {
      transform: none;
    }
  }

  .brand {
    display: inline-flex;
    align-items: center;
    min-width: 0;
  }

  .brand img {
    display: block;
    width: auto;
    height: 40px;
    object-fit: contain;
  }

  @media (max-width: 760px) {
    .brand img {
      height: 28px;
    }
  }

  @media (max-width: 767px) {
  .trustIndicators {
    display: grid;
    grid-template-columns: repeat(2, max-content);
    justify-content: center;
    column-gap: 18px;
    row-gap: 8px;
  }

  .trustIndicators li + li::before {
    display: none;
  }
}

/* Ultra-premium mobile trust indicators */
@media (max-width: 767px) {
  .trustIndicators {
    width: min(100%, 350px);
    margin: 18px auto 0;
    padding: 0;

    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    column-gap: 14px;
    row-gap: 10px;

    list-style: none;
  }

  .trustIndicators li {
    min-width: 0;
    min-height: 35px;
    padding: 0 11px;

    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 9px;

    color: rgba(255, 255, 255, 0.91);
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 14px;

    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.075) 0%,
        rgba(255, 255, 255, 0.035) 100%
      ),
      rgba(7, 12, 18, 0.52);

    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.09),
      0 8px 22px rgba(0, 0, 0, 0.16);

    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);

    font-size: 10.5px;
    font-weight: 590;
    line-height: 1;
    letter-spacing: 0.005em;
    white-space: nowrap;
  }

  .trustIndicators li::before,
  .trustIndicators li + li::before {
    content: "";
    display: block;

    width: 5px;
    height: 5px;
    margin: 0;
    flex: 0 0 5px;

    border-radius: 50%;
    background: #c8a660;

    box-shadow:
      0 0 0 4px rgba(200, 166, 96, 0.09),
      0 1px 5px rgba(200, 166, 96, 0.2);
  }
}

@media (max-width: 767px) {
  .menuBackdrop {
    position: fixed;
    inset: 0;
    z-index: 1390;

    display: block;

    background: rgba(2, 6, 10, 0.56);

    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);

    opacity: 0;
    visibility: hidden;
    pointer-events: none;

    transition:
      opacity 220ms ease,
      visibility 220ms ease;
  }

  .menuBackdrop.isOpen {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }

  .mobileMenu {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    z-index: 1400;

    width: min(88vw, 390px);
    padding:
      max(18px, env(safe-area-inset-top))
      22px
      max(24px, env(safe-area-inset-bottom));

    display: flex;
    flex-direction: column;

    color: #172029;

    background:
      radial-gradient(
        circle at 100% 0%,
        rgba(200, 166, 96, 0.12),
        transparent 34%
      ),
      rgba(249, 248, 245, 0.985);

    box-shadow: -24px 0 70px rgba(0, 0, 0, 0.24);

    transform: translateX(104%);
    visibility: hidden;

    transition:
      transform 300ms cubic-bezier(0.22, 1, 0.36, 1),
      visibility 300ms;
  }

  .mobileMenu.isOpen {
    transform: translateX(0);
    visibility: visible;
  }

  .mobileMenuHeader {
    min-height: 44px;

    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .mobileMenuBrand img {
    display: block;
    width: auto;
    height: 29px;

    filter: brightness(0);
    opacity: 0.88;
  }

  .menuClose {
    position: relative;

    width: 38px;
    height: 38px;
    padding: 0;

    border: 1px solid rgba(23, 32, 41, 0.12);
    border-radius: 50%;

    background: rgba(255, 255, 255, 0.68);

    cursor: pointer;
  }

  .menuClose span {
    position: absolute;
    top: 50%;
    left: 50%;

    width: 14px;
    height: 1px;

    background: #172029;
  }

  .menuClose span:first-child {
    transform: translate(-50%, -50%) rotate(45deg);
  }

  .menuClose span:last-child {
    transform: translate(-50%, -50%) rotate(-45deg);
  }

  .mobileMenuIntro {
    margin-top: 38px;
    padding-bottom: 23px;

    border-bottom: 1px solid rgba(23, 32, 41, 0.1);
  }

  .mobileMenuIntro span {
    color: #9b7a3c;

    font-size: 9px;
    font-weight: 720;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .mobileMenuIntro p {
    max-width: 270px;
    margin: 11px 0 0;

    color: #172029;

    font-size: 18px;
    font-weight: 590;
    line-height: 1.34;
    letter-spacing: -0.025em;
  }

  .mobileMenuNavigation {
    margin-top: 21px;
  }

  .mobileMenuNavigation a {
    min-height: 52px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    color: #172029;
    border-bottom: 1px solid rgba(23, 32, 41, 0.075);

    font-size: 16px;
    font-weight: 580;
    letter-spacing: -0.02em;
    text-decoration: none;
  }

  .mobileMenuNavigation a:first-child,
  .mobileMenuNavigation a:nth-child(2) {
    min-height: 57px;

    font-size: 20px;
    font-weight: 650;
  }

  .menuArrow {
    color: rgba(23, 32, 41, 0.42);
    font-size: 14px;
  }

  .mobileMenuDivider {
    height: 17px;

    border-bottom: 1px solid rgba(23, 32, 41, 0.12);
  }

  .mobileMenuFooter {
    margin-top: auto;
    padding-top: 24px;
  }

  .mobileMenuFooterLabel {
    display: block;
    margin-bottom: 9px;

    color: rgba(23, 32, 41, 0.5);

    font-size: 9px;
    font-weight: 680;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .mobilePartnerLink {
    padding: 16px 17px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;

    color: #f8f3e7;
    border: 1px solid rgba(200, 166, 96, 0.34);
    border-radius: 16px;

    background:
      linear-gradient(
        135deg,
        rgba(200, 166, 96, 0.16),
        rgba(200, 166, 96, 0.04)
      ),
      #111820;

    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.07),
      0 14px 30px rgba(11, 17, 23, 0.13);

    font-size: 13px;
    font-weight: 630;
    line-height: 1.25;
    text-decoration: none;
  }
}

</style>

<script>
  const menuToggle = document.querySelector(".menuToggle");
  const mobileMenu = document.querySelector(".mobileMenu");
  const menuBackdrop = document.querySelector(".menuBackdrop");
  const closeTriggers = document.querySelectorAll("[data-menu-close]");
  const header = document.querySelector(".nav");

  function openMobileMenu() {
    mobileMenu?.classList.add("isOpen");
    menuBackdrop?.classList.add("isOpen");

    document.documentElement.style.overflow = "hidden";

    menuToggle?.setAttribute("aria-expanded", "true");
    mobileMenu?.setAttribute("aria-hidden", "false");
  }

  function closeMobileMenu() {
    mobileMenu?.classList.remove("isOpen");
    menuBackdrop?.classList.remove("isOpen");

    document.documentElement.style.overflow = "";

    menuToggle?.setAttribute("aria-expanded", "false");
    mobileMenu?.setAttribute("aria-hidden", "true");
  }

  menuToggle?.addEventListener("click", openMobileMenu);

  closeTriggers.forEach((trigger) => {
    trigger.addEventListener("click", closeMobileMenu);
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeMobileMenu();
    }
  });

  function updateHeaderState() {
  header?.classList.toggle("isScrolled", window.scrollY > 18);
}

updateHeaderState();

window.addEventListener("scroll", updateHeaderState, {
  passive: true,
});
</script>
